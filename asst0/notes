2d array

linked list or array of linked lists

each node holds name or number position of parameter and value of the parameter

number would make it easier to find a specific parameter but name would use less memory due to comparisons
2d array

linked list or array of linked lists

each node holds name or number position of parameter and value of the parameter

number would make it easier to find a specific parameter but name would use less memory due to comparisons

